import ProfileUser from '../utils/userProfilePage';

const ProfileSeller = () => {
    
    return (
        <ProfileUser
            user_type_route={(-1)}
        />
    )
};

export default ProfileSeller;
