import ProfileUser from '../utils/userProfilePage';

const ProfileAdmin = () => {
    
    return (
        <ProfileUser
            user_type_route={(-1)}
        />
    )
};

export default ProfileAdmin;
