import UsePolicy from '../utils/usepolicy';

const CustomerPolicy = () => {
    
    return (
        <UsePolicy
            user_type_route={(-1)}
        />
    )
};

export default CustomerPolicy;
