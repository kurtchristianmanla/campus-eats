import ProfileUser from '../utils/userProfilePage';

const ProfileCustomer = () => {
    
    return (
        <ProfileUser
            user_type_route={(-1)}
        />
    )
};

export default ProfileCustomer;
